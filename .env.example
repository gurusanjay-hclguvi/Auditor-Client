# Copy to .env.local. API calls go to /api/sales-audit/..., which the Vite dev server proxies to
# DEV_API_TARGET: a local backend (http://127.0.0.1:8080) or the deployed one.
VITE_BASE_URL=/api
DEV_API_TARGET=http://127.0.0.1:8080
