# Copy to .env.local. API calls go directly to VITE_BASE_URL/sales-audit/...: a local backend
# (http://127.0.0.1:8080) or the deployed one. The backend must allow CORS from the dev origin.
VITE_BASE_URL=http://127.0.0.1:8080
